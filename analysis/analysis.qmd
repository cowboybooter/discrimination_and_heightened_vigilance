---
title: "Research Analysis Notebook"
---

```{r}
#| label: setup
#| include: false
library(here)
source(here("utils","check_packages.R"))
source(here("utils","functions.R"))
```

This quarto doc is used to analyze the data.

```{r}
#| label: loading-data

load(here("data", "data_constructed", "analytical_data.RData"))
```

```{r}
#| label: logit_model_without_survey_design_stuff

model <- glm(phq2_screener_result ~ disrespct, data = final_nhis, family = binomial)
summary(model)
```